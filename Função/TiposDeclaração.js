console.log(soma(3, 4)) 

//function declaration  // o programa carrega todas as funções declaradas antes de executar as linhas do código
function soma(x, y) {
    return x + y
}

//function expression
const sub = function (x, y){
    return x - y
}

//named function expression // pouco usado
const mult = function mult(x, y){
    return x * y
}